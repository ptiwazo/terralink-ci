# Identifiants PostgreSQL utilisés par docker-compose.
# Copier en `.env` dans ce dossier (infra/) avant `docker compose up`.
POSTGRES_USER=terralink
POSTGRES_PASSWORD=terralink
POSTGRES_DB=terralink
